fastapi==0.111.0
uvicorn==0.29.0
bittensor==11.0.0
# NOTE (2026-07-18): async-substrate-interface is intentionally NOT pinned
# here anymore — bittensor 11.0.0 (below) owns the substrate stack via
# bittensor-core and resolves its own compatible 2.x (cyscale) line. Pinning
# it ourselves to a 1.x now would conflict. Re-pin to the RESOLVED version
# once the 11.0.0 deploy is confirmed working (read it from _debug.decode),
# so a future redeploy can't drift again — that drift is exactly what broke
# /wallet under 10.1.0.
