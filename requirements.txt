fastapi==0.111.0
uvicorn==0.29.0
# bittensor 10.5.0 (2026-06-25) — the newest 10.x. Chosen over both 10.1.0
# (whose StakeInfo decoder int(netuid)'d on a tuple, 500ing /wallet after the
# chain runtime upgraded) and 11.0.0 (a ground-up rewrite that deletes the
# high-level API — get_stake_info_for_coldkey, all_subnets, get_metagraph_info,
# .substrate — this service is built on; verified live 2026-07-18). 10.5.0
# keeps that 10.x API while being recent enough to parse the current chain.
bittensor==10.5.0
# Pin async-substrate-interface to the EXACT resolved version, not bittensor
# 10.5.0's `>=2.0.4,<3.0.0` range. This is the whole lesson of the 2026-07-18
# outage: the original /wallet 500 came from this dep silently floating (1.5.x
# -> 1.6.4) on a Render redeploy and changing how StakeInfo decodes, while the
# service's own code never changed. bittensor is pinned exactly; pin its
# substrate stack exactly too so a rebuild can't drift again. 2.2.1 is the
# version that resolved under 10.5.0 and is verified working end-to-end
# (netuid decodes as int, all endpoints green). Bump deliberately alongside a
# bittensor upgrade, never implicitly. (10.5.0 uses cyscale, not scalecodec.)
async-substrate-interface==2.2.1
