fastapi==0.111.0
uvicorn==0.29.0
# bittensor 10.5.0 (2026-06-25) — the newest 10.x. Chosen over both 10.1.0
# (whose StakeInfo decoder int(netuid)'d on a tuple, 500ing /wallet after the
# chain runtime upgraded) and 11.0.0 (a ground-up rewrite that deletes the
# high-level API — get_stake_info_for_coldkey, all_subnets, get_metagraph_info,
# .substrate — this service is built on; verified live 2026-07-18). 10.5.0
# keeps that 10.x API while being recent enough to parse the current chain.
bittensor==10.5.0
# Do NOT pin async-substrate-interface here: bittensor 10.5.0 requires
# >=2.0.4,<3.0.0 (it migrated to the 2.x / cyscale line and dropped scalecodec,
# so the old scalecodec-vs-cyscale conflict that forced a <2.0 pin no longer
# applies). A leftover `<2.0.0` line here makes pip resolution UNSATISFIABLE and
# the Render build fails silently, leaving the previous container running — which
# is exactly what happened on the first 10.5.0 attempt. Let 10.5.0 resolve it,
# then re-pin to the RESOLVED exact 2.x version once confirmed (see _debug.decode).
